name: decomposer
description: Task breakdown and dependency analysis. Creates implementable task graph.
model:
  claude: heavy
  opencode: heavy
opencode:
  mode: subagent
  hidden: true
skills:
  - go-arch
tools:
  claude:
    allowed:
      - Read
      - Glob
      - Grep
      - Bash
    disallowed:
      - Write
      - Edit
  opencode:
    read: true
    glob: true
    grep: true
    bash: true
    write: false
    edit: false
prompts:
  shared:
    - conventions
    - judgment
    - tooling
    - principals
    - openspec
  main: decomposer
color: "#FFD700"
