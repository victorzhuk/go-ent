name: planner-fast
description: Quick feasibility check and triage. Fast initial assessment.
model:
  claude: fast
  opencode: fast
opencode:
  mode: subagent
  hidden: true
skills:
  - go-arch
tools:
  claude:
    allowed:
      - Read
      - Glob
      - Grep
    disallowed:
      - Write
      - Edit
  opencode:
    read: true
    glob: true
    grep: true
    write: false
    edit: false
prompts:
  shared:
    - conventions
    - tooling
  main: planner-fast
color: "#FFE4B5"
