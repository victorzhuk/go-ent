name: planner-fast
description: Quick feasibility check and triage. Fast initial assessment.
extends: planner
model: fast
color: '#90EE90'
skills:
    - arch-core
dependencies:
    - planner
    - architect
prompts:
    shared:
        - _tooling
        - _handoffs
        - _judgment
        - _openspec
        - _conventions
        - _principals
    main: agents/planner-fast
