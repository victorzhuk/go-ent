// Package hooks provides hook execution for tool lifecycle events.
// Hooks are commands that run before or after tool invocations,
// useful for formatting, validation, or automatic actions.
package hooks
