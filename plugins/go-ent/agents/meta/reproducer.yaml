name: reproducer
description: Create minimal bug reproductions. Write failing tests first.
model: fast
color: '#FF6347'
skills:
    - debug-core
    - go-test
toolPresets:
    - editing
dependencies:
    - researcher
    - debugger-fast
prompts:
    shared:
        - _handoffs
        - _judgment
        - _principals
        - _openspec
        - _tooling
        - _conventions
    main: agents/reproducer
